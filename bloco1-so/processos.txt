# Atividade 2 — O processo como abstração de execução
# Preencha este arquivo durante a atividade.
# ─────────────────────────────────────────────────────

# PARTE A — Inspecionar o processo atual
# Cole abaixo a saída de cada comando e anote o que observou.

echo $$
  PID do shell: 1234   # substitua pelo valor exibido no seu terminal

cat /proc/[PID]/status   (substitua [PID] pelo número real)
  PID: 1234
  PPID (processo pai): 1200
  State: S (sleeping)

cat /proc/[PID]/stat (campos relevantes)
  Campo 2 — nome do executável: (bash)
  Campo 3 — estado: S
  Campos 14 e 15 — utime / stime: 15 3

ps aux (primeiras linhas)
  USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
  root         1  0.0  0.1 169000 12000 ?        Ss   08:00   0:01 /sbin/init
  aluno     1200  0.0  0.2  12000  5000 pts/0    Ss   08:10   0:00 gnome-terminal
  aluno     1234  0.0  0.1   9000  3500 pts/0    S+   08:12   0:00 bash


# PARTE B — Processo filho
# Cole a saída dos comandos e responda:

sleep 5m &
  PID do filho: 1300

cat /proc/[FILHO]/status | grep -E "Pid|PPid|State"
  Pid:    1300
  PPid:   1234
  State:  S (sleeping)

ps -o pid,ppid,stat,cmd (antes do kill)
    PID  PPID STAT CMD
   1234  1200 S+   bash
   1300  1234 S    sleep 5m

ps -o pid,ppid,stat,cmd (depois do kill)
    PID  PPID STAT CMD
   1234  1200 S+   bash


# PARTE C — Questões de síntese

# 1. O campo PPID em /proc/$$/status indica o processo pai do shell.
#    Execute ps -o pid,ppid,cmd e localize o par shell -> processo pai.
#    O que esse encadeamento sugere sobre como os processos sao
#    criados no Linux?

Resposta 1:
O encadeamento mostra que os processos são criados a partir de outros processos. 
Cada processo possui um processo pai que o iniciou, formando uma árvore de processos no sistema.


# 2. O arquivo /proc/<PID>/status mostra o campo State com uma letra
#    (S, R, Z...). Depois de executar kill $FILHO e rodar ps novamente,
#    o que aconteceu com o processo filho? O que isso te faz pensar
#    sobre a vida de um processo?

Resposta:
Depois do comando kill, o processo filho deixou de aparecer no ps, indicando que foi encerrado.
Isso mostra que processos possuem um ciclo de vida: são criados, executam tarefas e depois terminam.
